<?xml version="1.0" encoding="utf-8"?>

<!-- Main layout for SleepActivity screen -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Back button to return to previous screen -->
    <Button
        android:id="@+id/btnBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Back" />

    <!-- Button to start tracking sleep -->
    <Button
        android:id="@+id/btnStartSleep"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Start Sleep"
        android:layout_marginTop="16dp" />

    <!-- Button to end sleep session -->
    <!-- Disabled by default until Start is pressed -->
    <Button
        android:id="@+id/btnEndSleep"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="End Sleep"
        android:enabled="false" />

    <!-- Rating bar for user to rate sleep quality (1–5) -->
    <RatingBar
        android:id="@+id/ratingBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:numStars="5"
        android:stepSize="1" />

    <!-- Optional notes about sleep -->
    <EditText
        android:id="@+id/etNote"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Sleep notes (optional)" />

    <!-- Section header for sleep history list -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sleep History"
        android:textStyle="bold"
        android:paddingTop="12dp"
        android:paddingBottom="6dp" />

    <!-- RecyclerView displaying all previous sleep entries -->
    <!-- layout_weight=1 makes it fill remaining screen space -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerSleep"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
