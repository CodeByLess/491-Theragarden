<?xml version="1.0" encoding="utf-8"?>

<!-- ScrollView allows the page to scroll if the content is taller than the screen -->
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:fitsSystemWindows="true"
    android:paddingTop="24dp"
    android:background="@color/dark_gray_brown"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- LinearLayout arranges all elements vertically -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:orientation="vertical">

        <!-- Back button to return to the previous screen -->
        <Button
            android:id="@+id/btnBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Back"
            android:backgroundTint="@color/gray_green"
            android:textColor="@color/white"
            android:layout_marginBottom="12dp"/>

        <!-- Title of the page -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Submit an Issue"
            android:textColor="@color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            android:paddingBottom="12dp" />

        <!-- Input field for issue title -->
        <EditText
            android:id="@+id/etTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Title (required)"
            android:textColor="@color/white"
            android:textColorHint="@color/white"
            android:inputType="textCapSentences"
            android:maxLength="80" />

        <!-- Input field for issue description -->
        <EditText
            android:id="@+id/etDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Describe the issue (required)"
            android:textColor="@color/white"
            android:textColorHint="@color/white"
            android:inputType="textMultiLine"
            android:minLines="5"
            android:gravity="top"
            android:layout_marginTop="12dp" />

        <!-- Label for issue category -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:text="Category"
            android:layout_marginTop="12dp" />

        <!-- Dropdown menu for selecting issue category -->
        <Spinner
            android:id="@+id/spCategory"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Label for issue priority -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:text="Priority"
            android:layout_marginTop="12dp" />

        <!-- Dropdown menu for selecting issue priority -->
        <Spinner
            android:id="@+id/spPriority"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Progress bar shown while the issue is being submitted -->
        <ProgressBar
            android:id="@+id/progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:layout_gravity="center"
            android:layout_marginTop="16dp" />

        <!-- Button used to submit the issue to Firebase -->
        <Button
            android:id="@+id/btnSubmit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Submit"
            android:layout_marginTop="16dp"
            android:backgroundTint="@color/gray_green"
            android:textColor="@color/white"/>

        <!-- Displays success or error messages after submission -->
        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:text=""
            android:layout_marginTop="10dp" />

    </LinearLayout>
</ScrollView>